/* chemin : /frontend/src/app/eventi/[id]/ClientEventoPage.tsx */
"use client";

/**
 * Questo è un componente template legacy.
 * Il vero contenuto è in page.tsx (Server Component wrapper).
 * Questo file esiste per compatibilità e non viene usato attualmente.
 */

export default function ClientEventoPage({ id }: { id: string }) {
  return null;
}
